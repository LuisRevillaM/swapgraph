# M144 — Payment Proofs + Execution Grants

Goal: add non-custodial settlement proofing and one-time execution grants with deterministic replay protection.

Delivered:
- `POST /market/deals/{deal_id}/payment-proof`
- `POST /market/execution-grants`
- `POST /market/execution-grants/{grant_id}/consume`
- dual-attestation payment proof flow
- proof fingerprint replay protection
- one-time audience-scoped execution grant consume
- `scripts/run-m144-market-execution-grants-payment-proof-scenario.mjs`

Gate:
- unattested proof cannot complete a deal
- replayed proofs/grants fail deterministically
- scope mismatch on grant consume fails deterministically
