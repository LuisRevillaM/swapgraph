# M129 — Matcher runner seam extraction parity gate (non-breaking)

## Objective
Extract matcher invocation seams from `marketplaceMatchingService` into dedicated runner helper modules and enforce JS/TS parity with deterministic extraction verification.

## Scope
- Add extracted JS runner helper module:
  - `src/service/marketplaceMatcherRunners.mjs`
- Add concrete TS parity runner module:
  - `src-ts/service/marketplaceMatcherRunners.mts`
- Rewire service to import matcher runner helper functions:
  - `src/service/marketplaceMatchingService.mjs`
- Add deterministic M129 parity/extraction verifier:
  - `scripts/run-m129-typescript-service-runner-parity-scenario.mjs`
  - `fixtures/release/m129_scenario.json`
  - `fixtures/release/m129_expected.json`
  - `verify/m129.sh`
  - `milestones/M129.yaml`

## Non-goals (M129)
- No API/schema changes.
- No runtime cutover to TS service execution.
- No matcher algorithm changes.
- No canary/rollback policy changes.

## Acceptance criteria
- `npm run verify:m129` exits 0.
- `npm run verify:m128` exits 0 (composed dependency gate).
- `npm run verify:m125` exits 0 (runtime behavior dependency gate).
- Verifier proves:
  - TS runner helper module is concrete (no export-star wrapper).
  - JS/TS runner helper export surfaces match expected function set.
  - normalized parity hash matches between JS and TS runner helper modules.
  - extracted runner functions are imported into service and no longer locally defined.

## Required artifacts
- `docs/prd/M129.md`
- `src/service/marketplaceMatcherRunners.mjs`
- `src-ts/service/marketplaceMatcherRunners.mts`
- `src/service/marketplaceMatchingService.mjs`
- `scripts/run-m129-typescript-service-runner-parity-scenario.mjs`
- `fixtures/release/m129_scenario.json`
- `fixtures/release/m129_expected.json`
- `verify/m129.sh`
- `milestones/M129.yaml`
- `artifacts/milestones/M129/latest/typescript_service_runner_parity_output.json`
- `artifacts/milestones/M129/latest/assertions.json`
- `artifacts/milestones/M129/latest/commands.log`
