# M133 — State bootstrap + edge-intent helper extraction parity gate (non-breaking)

## Objective
Extract state bootstrap + edge-intent helpers from `marketplaceMatchingService` into dedicated modules and enforce JS/TS parity with deterministic extraction verification.

## Scope
- Add extracted JS helper module:
  - `src/service/marketplaceMatchingStateHelpers.mjs`
- Add concrete TS parity helper module:
  - `src-ts/service/marketplaceMatchingStateHelpers.mts`
- Rewire service to import state/bootstrap helpers:
  - `src/service/marketplaceMatchingService.mjs`
- Add deterministic M133 parity/extraction verifier:
  - `scripts/run-m133-typescript-service-state-helper-parity-scenario.mjs`
  - `fixtures/release/m133_scenario.json`
  - `fixtures/release/m133_expected.json`
  - `verify/m133.sh`
  - `milestones/M133.yaml`

## Non-goals (M133)
- No API/schema changes.
- No runtime cutover to TS service execution.
- No matcher algorithm changes.
- No canary/rollback policy changes.

## Acceptance criteria
- `npm run verify:m133` exits 0.
- `npm run verify:m132` exits 0 (composed dependency gate).
- `npm run verify:m111` exits 0 (runtime behavior dependency gate).
- Verifier proves:
  - TS state helper module is concrete (no export-star wrapper).
  - JS/TS helper export surfaces match expected function set.
  - normalized parity hash matches between JS and TS helper modules.
  - extracted state/bootstrap functions are imported into service and no longer locally defined there.

## Required artifacts
- `docs/prd/M133.md`
- `src/service/marketplaceMatchingStateHelpers.mjs`
- `src-ts/service/marketplaceMatchingStateHelpers.mts`
- `src/service/marketplaceMatchingService.mjs`
- `scripts/run-m133-typescript-service-state-helper-parity-scenario.mjs`
- `fixtures/release/m133_scenario.json`
- `fixtures/release/m133_expected.json`
- `verify/m133.sh`
- `milestones/M133.yaml`
- `artifacts/milestones/M133/latest/typescript_service_state_helper_parity_output.json`
- `artifacts/milestones/M133/latest/assertions.json`
- `artifacts/milestones/M133/latest/commands.log`
