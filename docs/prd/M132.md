# M132 — Proposal lifecycle helper extraction parity gate (non-breaking)

## Objective
Extract proposal lifecycle helpers from `marketplaceMatchingService` into dedicated modules and enforce JS/TS parity with deterministic extraction verification.

## Scope
- Add extracted JS helper module:
  - `src/service/marketplaceMatchingProposalLifecycleHelpers.mjs`
- Add concrete TS parity helper module:
  - `src-ts/service/marketplaceMatchingProposalLifecycleHelpers.mts`
- Rewire service to import proposal lifecycle helpers:
  - `src/service/marketplaceMatchingService.mjs`
- Add deterministic M132 parity/extraction verifier:
  - `scripts/run-m132-typescript-service-proposal-lifecycle-helper-parity-scenario.mjs`
  - `fixtures/release/m132_scenario.json`
  - `fixtures/release/m132_expected.json`
  - `verify/m132.sh`
  - `milestones/M132.yaml`

## Non-goals (M132)
- No API/schema changes.
- No runtime cutover to TS service execution.
- No matcher algorithm changes.
- No canary/rollback policy changes.

## Acceptance criteria
- `npm run verify:m132` exits 0.
- `npm run verify:m131` exits 0 (composed dependency gate).
- `npm run verify:m111` exits 0 (runtime behavior dependency gate).
- Verifier proves:
  - TS proposal lifecycle helper module is concrete (no export-star wrapper).
  - JS/TS helper export surfaces match expected function set.
  - normalized parity hash matches between JS and TS helper modules.
  - extracted lifecycle functions are imported into service and no longer locally defined there.

## Required artifacts
- `docs/prd/M132.md`
- `src/service/marketplaceMatchingProposalLifecycleHelpers.mjs`
- `src-ts/service/marketplaceMatchingProposalLifecycleHelpers.mts`
- `src/service/marketplaceMatchingService.mjs`
- `scripts/run-m132-typescript-service-proposal-lifecycle-helper-parity-scenario.mjs`
- `fixtures/release/m132_scenario.json`
- `fixtures/release/m132_expected.json`
- `verify/m132.sh`
- `milestones/M132.yaml`
- `artifacts/milestones/M132/latest/typescript_service_proposal_lifecycle_helper_parity_output.json`
- `artifacts/milestones/M132/latest/assertions.json`
- `artifacts/milestones/M132/latest/commands.log`
