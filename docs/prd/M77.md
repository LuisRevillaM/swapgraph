# M77 — Billing statement export contract (fixtures-first)

## Objective
Deliver deterministic, signed commercial-governance surfaces for milestone M77.

## Scope
- Extend API/contracts and schemas for M77 forecast scope.
- Add deterministic scenario + expected fixture proof.
- Enforce signature/tamper verification where export payloads are signed.

## Acceptance
- `npm run verify:m77` exits 0.
- `node verify/runner.ts milestones/M77.yaml` returns `overall=true`.
- Artifacts available under `artifacts/milestones/M77/latest/*`.
