# M130 — Canary/rollback state helper extraction parity gate (non-breaking)

## Objective
Extract canary bucket + rollback state helpers from `marketplaceMatchingService` into dedicated modules and enforce JS/TS parity with deterministic extraction verification.

## Scope
- Add extracted JS helper module:
  - `src/service/marketplaceMatchingCanaryHelpers.mjs`
- Add concrete TS parity helper module:
  - `src-ts/service/marketplaceMatchingCanaryHelpers.mts`
- Rewire service to import canary/rollback helpers:
  - `src/service/marketplaceMatchingService.mjs`
- Add deterministic M130 parity/extraction verifier:
  - `scripts/run-m130-typescript-service-canary-helper-parity-scenario.mjs`
  - `fixtures/release/m130_scenario.json`
  - `fixtures/release/m130_expected.json`
  - `verify/m130.sh`
  - `milestones/M130.yaml`

## Non-goals (M130)
- No API/schema changes.
- No runtime cutover to TS service execution.
- No matcher algorithm changes.
- No canary/rollback policy semantic changes.

## Acceptance criteria
- `npm run verify:m130` exits 0.
- `npm run verify:m129` exits 0 (composed dependency gate).
- `npm run verify:m119` exits 0 (canary runtime behavior dependency gate).
- Verifier proves:
  - TS canary helper module is concrete (no export-star wrapper).
  - JS/TS canary helper export surfaces match expected function set.
  - normalized parity hash matches between JS and TS canary helper modules.
  - extracted canary functions are imported into service and no longer locally defined there.

## Required artifacts
- `docs/prd/M130.md`
- `src/service/marketplaceMatchingCanaryHelpers.mjs`
- `src-ts/service/marketplaceMatchingCanaryHelpers.mts`
- `src/service/marketplaceMatchingService.mjs`
- `scripts/run-m130-typescript-service-canary-helper-parity-scenario.mjs`
- `fixtures/release/m130_scenario.json`
- `fixtures/release/m130_expected.json`
- `verify/m130.sh`
- `milestones/M130.yaml`
- `artifacts/milestones/M130/latest/typescript_service_canary_helper_parity_output.json`
- `artifacts/milestones/M130/latest/assertions.json`
- `artifacts/milestones/M130/latest/commands.log`
