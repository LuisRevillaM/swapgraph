# M109 — Partner liquidity-provider onboarding and governance contracts (PRD-only, discovery-first)

## Objective
Define partner LP onboarding, rollout, and governance contracts so external liquidity providers can be integrated safely under trust, commercial, and operational controls.

## Scope
### A) Partner LP onboarding contracts (to define)
1. `partnerLiquidityProvider.onboard` → `POST /partner-liquidity-providers`
2. `partnerLiquidityProvider.get` → `GET /partner-liquidity-providers/{provider_id}`
3. `partnerLiquidityProvider.status.upsert` → `POST /partner-liquidity-providers/{provider_id}/status`

### B) Eligibility and rollout contracts (to define)
1. `partnerLiquidityProvider.eligibility.evaluate` → `POST /partner-liquidity-providers/{provider_id}/eligibility/evaluate`
2. `partnerLiquidityProvider.rollout.upsert` → `POST /partner-liquidity-providers/{provider_id}/rollout`
3. `partnerLiquidityProvider.rollout.export` → `GET /partner-liquidity-providers/{provider_id}/rollout/export`

### C) Governance requirements
- Segment-aware access according to approved partner segmentation policy.
- Explicit downgrade/offboarding triggers for trust/reliability violations.
- Signed governance export for auditability.

## Upstream BRD dependencies
- BRD-03 commercial packaging strategy: `docs/brd/2026-02-21_BRD-03_commercial-packaging-strategy.md`
- BRD-04 partner segmentation/rollout strategy: `docs/brd/2026-02-21_BRD-04_partner-segmentation-rollout-strategy.md`
- BRD-02 trust/safety operating posture: `docs/brd/2026-02-21_BRD-02_trust-safety-operating-policy.md`

## Deterministic reason-code floor (proposed)
- `partner_liquidity_provider_invalid`
- `partner_liquidity_provider_not_found`
- `partner_liquidity_provider_eligibility_failed`
- `partner_liquidity_provider_rollout_blocked`
- `partner_liquidity_provider_downgrade_required`

## Non-goals (M109)
- No payment settlement implementation changes.
- No partner legal template automation.
- No production onboarding rollout in this milestone.

## Acceptance (PRD stage)
M109 is accepted when:
1. onboarding + eligibility + rollout contract inventory is explicit,
2. BRD-driven governance dependencies are resolved or deferred explicitly,
3. downgrade/offboarding policy boundaries are explicit,
4. deterministic reason-code floor is approved.

## Acceptance (future implementation stage)
- `npm run verify:m109` exits 0.
- `node verify/runner.ts milestones/M109.yaml` returns `overall=true`.
- Artifacts under `artifacts/milestones/M109/latest/*`.
