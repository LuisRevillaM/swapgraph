# M136 — Canary decision helper extraction parity gate (non-breaking)

## Objective
Extract canary decision/sample assembly utilities from `marketplaceMatchingService` into dedicated modules and enforce JS/TS parity with deterministic extraction verification.

## Scope
- Add extracted JS helper module:
  - `src/service/marketplaceMatchingCanaryDecisionHelpers.mjs`
- Add concrete TS parity helper module:
  - `src-ts/service/marketplaceMatchingCanaryDecisionHelpers.mts`
- Rewire service to import canary decision helpers:
  - `src/service/marketplaceMatchingService.mjs`
- Add deterministic M136 parity/extraction verifier:
  - `scripts/run-m136-typescript-service-canary-decision-helper-parity-scenario.mjs`
  - `fixtures/release/m136_scenario.json`
  - `fixtures/release/m136_expected.json`
  - `verify/m136.sh`
  - `milestones/M136.yaml`

## Non-goals (M136)
- No API/schema changes.
- No runtime cutover to TS service execution.
- No matcher algorithm changes.
- No canary/rollback policy semantic changes.

## Acceptance criteria
- `npm run verify:m136` exits 0.
- `npm run verify:m135` exits 0 (composed dependency gate).
- `npm run verify:m119` exits 0 (canary runtime behavior dependency gate).
- Verifier proves:
  - TS canary decision helper module is concrete (no export-star wrapper).
  - JS/TS helper export surfaces match expected function set.
  - normalized parity hash matches between JS and TS canary decision helper modules.
  - extracted canary decision functions are imported into service.

## Required artifacts
- `docs/prd/M136.md`
- `src/service/marketplaceMatchingCanaryDecisionHelpers.mjs`
- `src-ts/service/marketplaceMatchingCanaryDecisionHelpers.mts`
- `src/service/marketplaceMatchingService.mjs`
- `scripts/run-m136-typescript-service-canary-decision-helper-parity-scenario.mjs`
- `fixtures/release/m136_scenario.json`
- `fixtures/release/m136_expected.json`
- `verify/m136.sh`
- `milestones/M136.yaml`
- `artifacts/milestones/M136/latest/typescript_service_canary_decision_helper_parity_output.json`
- `artifacts/milestones/M136/latest/assertions.json`
- `artifacts/milestones/M136/latest/commands.log`
