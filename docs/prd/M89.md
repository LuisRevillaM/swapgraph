# M89 — Tier-2 adapter capability contract (cross-ecosystem preflight, fixtures-first)

## Objective
Deliver deterministic Tier-2 adapter capability contracts and cross-ecosystem preflight checks for non-Steam secondary ecosystem handoff.

## Scope
- Add idempotent Tier-2 capability upsert/read contract surfaces.
- Add idempotent Tier-2 preflight checks for ecosystem pairing, transfer-primitive support, route-hop ceilings, and dry-run policy.
- Add fixture scenario proving idempotency replay/mismatch and scope enforcement.

## Acceptance
- `npm run verify:m89` exits 0.
- `node verify/runner.mjs milestones/M89.yaml` returns `overall=true`.
- Artifacts available under `artifacts/milestones/M89/latest/*`.
