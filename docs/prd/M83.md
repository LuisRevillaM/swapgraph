# M83 — Dispute workflow and evidence-bundle contracts (fixtures-first)

## Objective
Deliver deterministic dispute lifecycle and signed evidence-bundle export contracts for milestone M83.

## Scope
- Add idempotent dispute create/resolve API contracts.
- Add signed, paginated evidence-bundle export for disputes.
- Add deterministic fixture scenario including signature verification and tamper-fail proofs.

## Acceptance
- `npm run verify:m83` exits 0.
- `node verify/runner.ts milestones/M83.yaml` returns `overall=true`.
- Artifacts available under `artifacts/milestones/M83/latest/*`.
