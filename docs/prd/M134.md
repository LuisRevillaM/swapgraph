# M134 — Response/error/idempotency helper extraction parity gate (non-breaking)

## Objective
Extract response/error/idempotency utilities from `marketplaceMatchingService` into dedicated modules and enforce JS/TS parity with deterministic extraction verification.

## Scope
- Add extracted JS helper module:
  - `src/service/marketplaceMatchingResponseHelpers.mjs`
- Add concrete TS parity helper module:
  - `src-ts/service/marketplaceMatchingResponseHelpers.mts`
- Rewire service to import response/error/idempotency helpers:
  - `src/service/marketplaceMatchingService.mjs`
- Add deterministic M134 parity/extraction verifier:
  - `scripts/run-m134-typescript-service-response-helper-parity-scenario.mjs`
  - `fixtures/release/m134_scenario.json`
  - `fixtures/release/m134_expected.json`
  - `verify/m134.sh`
  - `milestones/M134.yaml`

## Non-goals (M134)
- No API/schema changes.
- No runtime cutover to TS service execution.
- No matcher algorithm changes.
- No canary/rollback policy changes.

## Acceptance criteria
- `npm run verify:m134` exits 0.
- `npm run verify:m133` exits 0 (composed dependency gate).
- `npm run verify:m111` exits 0 (runtime behavior dependency gate).
- Verifier proves:
  - TS response helper module is concrete (no export-star wrapper).
  - JS/TS helper export surfaces match expected function set.
  - normalized parity hash matches between JS and TS helper modules.
  - extracted response/error/idempotency functions are imported into service and no longer locally defined there.

## Required artifacts
- `docs/prd/M134.md`
- `src/service/marketplaceMatchingResponseHelpers.mjs`
- `src-ts/service/marketplaceMatchingResponseHelpers.mts`
- `src/service/marketplaceMatchingService.mjs`
- `scripts/run-m134-typescript-service-response-helper-parity-scenario.mjs`
- `fixtures/release/m134_scenario.json`
- `fixtures/release/m134_expected.json`
- `verify/m134.sh`
- `milestones/M134.yaml`
- `artifacts/milestones/M134/latest/typescript_service_response_helper_parity_output.json`
- `artifacts/milestones/M134/latest/assertions.json`
- `artifacts/milestones/M134/latest/commands.log`
