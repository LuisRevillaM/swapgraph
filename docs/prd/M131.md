# M131 — Request/asset normalization helper extraction parity gate (non-breaking)

## Objective
Extract request + asset normalization helpers from `marketplaceMatchingService` into dedicated modules and enforce JS/TS parity with deterministic extraction verification.

## Scope
- Add extracted JS helper module:
  - `src/service/marketplaceMatchingRequestHelpers.mjs`
- Add concrete TS parity helper module:
  - `src-ts/service/marketplaceMatchingRequestHelpers.mts`
- Rewire service to import normalization helpers:
  - `src/service/marketplaceMatchingService.mjs`
- Add deterministic M131 parity/extraction verifier:
  - `scripts/run-m131-typescript-service-request-helper-parity-scenario.mjs`
  - `fixtures/release/m131_scenario.json`
  - `fixtures/release/m131_expected.json`
  - `verify/m131.sh`
  - `milestones/M131.yaml`

## Non-goals (M131)
- No API/schema changes.
- No runtime cutover to TS service execution.
- No matcher algorithm changes.
- No canary/rollback policy changes.

## Acceptance criteria
- `npm run verify:m131` exits 0.
- `npm run verify:m130` exits 0 (composed dependency gate).
- `npm run verify:m111` exits 0 (runtime behavior dependency gate).
- Verifier proves:
  - TS request helper module is concrete (no export-star wrapper).
  - JS/TS helper export surfaces match expected function set.
  - normalized parity hash matches between JS and TS helper modules.
  - extracted functions are imported into service and no longer locally defined there.

## Required artifacts
- `docs/prd/M131.md`
- `src/service/marketplaceMatchingRequestHelpers.mjs`
- `src-ts/service/marketplaceMatchingRequestHelpers.mts`
- `src/service/marketplaceMatchingService.mjs`
- `scripts/run-m131-typescript-service-request-helper-parity-scenario.mjs`
- `fixtures/release/m131_scenario.json`
- `fixtures/release/m131_expected.json`
- `verify/m131.sh`
- `milestones/M131.yaml`
- `artifacts/milestones/M131/latest/typescript_service_request_helper_parity_output.json`
- `artifacts/milestones/M131/latest/assertions.json`
- `artifacts/milestones/M131/latest/commands.log`
