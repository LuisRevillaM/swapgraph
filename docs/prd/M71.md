# M71 — Rollout diagnostics automation continuity-window hardening (fixtures-first)

## Objective
Add deterministic continuity-window anchors to diagnostics automation execution attestation so continuation context has explicit time-bounded semantics.

## Scope
- Extend `automation_hints.execution_attestation` with:
  - `continuation_window_minutes`
  - `continuation_expires_at`
- Derive deterministically from signed diagnostics context:
  - window from query (`continuation_window_minutes`, default `30`)
  - expiry from `exported_at + continuation_window_minutes`
- Bind continuity-window fields into:
  - `attestation_hash`
  - `continuation_hash`
- Enforce verifier parity across default and public-key verification paths.

## Acceptance
- `npm run verify:m71` exits 0.
- `node verify/runner.ts milestones/M71.yaml` returns `overall=true`.
- Artifacts available under `artifacts/milestones/M71/latest/*`.
