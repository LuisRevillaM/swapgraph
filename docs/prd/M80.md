# M80 — Oauth client lifecycle contract (fixtures-first)

## Objective
Deliver deterministic, signed commercial-governance surfaces for milestone M80.

## Scope
- Extend API/contracts and schemas for M80 forecast scope.
- Add deterministic scenario + expected fixture proof.
- Enforce signature/tamper verification where export payloads are signed.

## Acceptance
- `npm run verify:m80` exits 0.
- `node verify/runner.ts milestones/M80.yaml` returns `overall=true`.
- Artifacts available under `artifacts/milestones/M80/latest/*`.
