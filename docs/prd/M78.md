# M78 — Sla policy + breach event contracts (fixtures-first)

## Objective
Deliver deterministic, signed commercial-governance surfaces for milestone M78.

## Scope
- Extend API/contracts and schemas for M78 forecast scope.
- Add deterministic scenario + expected fixture proof.
- Enforce signature/tamper verification where export payloads are signed.

## Acceptance
- `npm run verify:m78` exits 0.
- `node verify/runner.ts milestones/M78.yaml` returns `overall=true`.
- Artifacts available under `artifacts/milestones/M78/latest/*`.
