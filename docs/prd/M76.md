# M76 — Commercial usage ledger normalization (fixtures-first)

## Objective
Deliver deterministic, signed commercial-governance surfaces for milestone M76.

## Scope
- Extend API/contracts and schemas for M76 forecast scope.
- Add deterministic scenario + expected fixture proof.
- Enforce signature/tamper verification where export payloads are signed.

## Acceptance
- `npm run verify:m76` exits 0.
- `node verify/runner.ts milestones/M76.yaml` returns `overall=true`.
- Artifacts available under `artifacts/milestones/M76/latest/*`.
