# M79 — Partner dashboard summary api contract (fixtures-first)

## Objective
Deliver deterministic, signed commercial-governance surfaces for milestone M79.

## Scope
- Extend API/contracts and schemas for M79 forecast scope.
- Add deterministic scenario + expected fixture proof.
- Enforce signature/tamper verification where export payloads are signed.

## Acceptance
- `npm run verify:m79` exits 0.
- `node verify/runner.ts milestones/M79.yaml` returns `overall=true`.
- Artifacts available under `artifacts/milestones/M79/latest/*`.
