# M139 — Canary routing helper extraction parity gate (non-breaking)

## Objective
Extract canary routing/bootstrap utilities from `marketplaceMatchingService` into dedicated modules and enforce JS/TS parity with deterministic extraction verification.

## Scope
- Add extracted JS helper module:
  - `src/service/marketplaceMatchingCanaryRoutingHelpers.mjs`
- Add concrete TS parity helper module:
  - `src-ts/service/marketplaceMatchingCanaryRoutingHelpers.mts`
- Rewire service to import canary routing helper:
  - `src/service/marketplaceMatchingService.mjs`
- Add deterministic M139 parity/extraction verifier:
  - `scripts/run-m139-typescript-service-canary-routing-helper-parity-scenario.mjs`
  - `fixtures/release/m139_scenario.json`
  - `fixtures/release/m139_expected.json`
  - `verify/m139.sh`
  - `milestones/M139.yaml`

## Non-goals (M139)
- No API/schema changes.
- No runtime cutover to TS service execution.
- No matcher algorithm changes.
- No canary/rollback policy semantic changes.

## Acceptance criteria
- `npm run verify:m139` exits 0.
- `npm run verify:m138` exits 0 (composed dependency gate).
- `npm run verify:m119` exits 0 (canary runtime behavior dependency gate).
- Verifier proves:
  - TS canary routing helper module is concrete (no export-star wrapper).
  - JS/TS helper export surfaces match expected function set.
  - normalized parity hash matches between JS and TS canary routing helper modules.
  - extracted canary routing helper function is imported into service.

## Required artifacts
- `docs/prd/M139.md`
- `src/service/marketplaceMatchingCanaryRoutingHelpers.mjs`
- `src-ts/service/marketplaceMatchingCanaryRoutingHelpers.mts`
- `src/service/marketplaceMatchingService.mjs`
- `scripts/run-m139-typescript-service-canary-routing-helper-parity-scenario.mjs`
- `fixtures/release/m139_scenario.json`
- `fixtures/release/m139_expected.json`
- `verify/m139.sh`
- `milestones/M139.yaml`
- `artifacts/milestones/M139/latest/typescript_service_canary_routing_helper_parity_output.json`
- `artifacts/milestones/M139/latest/assertions.json`
- `artifacts/milestones/M139/latest/commands.log`
