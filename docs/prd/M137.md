# M137 — Execution helper extraction parity gate (non-breaking)

## Objective
Extract v2 canary/shadow execution utilities from `marketplaceMatchingService` into dedicated modules and enforce JS/TS parity with deterministic extraction verification.

## Scope
- Add extracted JS helper module:
  - `src/service/marketplaceMatchingExecutionHelpers.mjs`
- Add concrete TS parity helper module:
  - `src-ts/service/marketplaceMatchingExecutionHelpers.mts`
- Rewire service to import execution helpers:
  - `src/service/marketplaceMatchingService.mjs`
- Add deterministic M137 parity/extraction verifier:
  - `scripts/run-m137-typescript-service-execution-helper-parity-scenario.mjs`
  - `fixtures/release/m137_scenario.json`
  - `fixtures/release/m137_expected.json`
  - `verify/m137.sh`
  - `milestones/M137.yaml`

## Non-goals (M137)
- No API/schema changes.
- No runtime cutover to TS service execution.
- No matcher algorithm changes.
- No canary/rollback policy semantic changes.

## Acceptance criteria
- `npm run verify:m137` exits 0.
- `npm run verify:m136` exits 0 (composed dependency gate).
- `npm run verify:m119` exits 0 (canary runtime behavior dependency gate).
- Verifier proves:
  - TS execution helper module is concrete (no export-star wrapper).
  - JS/TS helper export surfaces match expected function set.
  - normalized parity hash matches between JS and TS execution helper modules.
  - extracted execution helper functions are imported into service.

## Required artifacts
- `docs/prd/M137.md`
- `src/service/marketplaceMatchingExecutionHelpers.mjs`
- `src-ts/service/marketplaceMatchingExecutionHelpers.mts`
- `src/service/marketplaceMatchingService.mjs`
- `scripts/run-m137-typescript-service-execution-helper-parity-scenario.mjs`
- `fixtures/release/m137_scenario.json`
- `fixtures/release/m137_expected.json`
- `verify/m137.sh`
- `milestones/M137.yaml`
- `artifacts/milestones/M137/latest/typescript_service_execution_helper_parity_output.json`
- `artifacts/milestones/M137/latest/assertions.json`
- `artifacts/milestones/M137/latest/commands.log`
