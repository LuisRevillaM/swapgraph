#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
node "$SCRIPT_DIR/run-swift-filter-check.mjs" \
  --check-id SC-UX-07 \
  --item "Notification preferences persist durable category toggles::NotificationPreferencesFeatureTests/testPreferencesStorePersistsValues" \
  --item "Preferences alter routing filter behavior deterministically::NotificationPreferencesFeatureTests/testPreferencesViewModelPersistsAndAffectsRoutingFilterBehavior"
