#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
node "$SCRIPT_DIR/run-swift-filter-check.mjs" \
  --check-id SC-RL-04 \
  --item "Conflict envelope maps to blocked fallback state::ActiveFeatureTests/testConflictErrorMapsBlockedFallbackWithEnvelopeDetails" \
  --item "Offline refresh recovery path keeps cached continuity::OfflineResilienceFeatureTests/testActiveRefreshFallsBackToCachedSnapshotWhenOffline"
